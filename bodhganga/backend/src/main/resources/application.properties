spring.application.name=bodhganga

# Server Configuration
# NOTE: BodhGanga runs on port 9090 (app traffic + management)
server.port=${PORT:9090}

# MongoDB Configuration
spring.data.mongodb.uri=${MONGO_URI:${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/bodhganga}}
spring.data.mongodb.auto-index-creation=true

# JWT Configuration — MUST be set via environment variable in production
# Generate a strong secret: openssl rand -base64 64
jwt.secret=${JWT_SECRET:test-only-secret-key-minimum-64-characters-long-replace-in-production-env}
jwt.expiration=${JWT_EXPIRATION:86400000}

# OTP Configuration (MSG91)
otp.enabled=${OTP_ENABLED:true}

# Payment Gateway (Razorpay) — set via environment
razorpay.key.id=${RAZORPAY_KEY_ID:}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:}

# Cloud Storage (AWS S3) — set via environment
aws.access.key.id=${AWS_ACCESS_KEY_ID:mock-access-key-id-for-dev}
aws.secret.access.key=${AWS_SECRET_ACCESS_KEY:mock-secret-access-key-for-dev}
aws.region=${AWS_REGION:ap-south-1}
aws.s3.bucket.name=${AWS_S3_BUCKET:bodhganga-prod}

# Email Integration (SMTP) — set via environment
spring.mail.host=${SMTP_HOST:smtp.gmail.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USER:}
spring.mail.password=${SMTP_PASS:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# CORS — comma-separated list of allowed origins for production
# Example: ALLOWED_ORIGINS=https://bodhganga.in,https://www.bodhganga.in
# Leave unset for localhost-only (development)

# =============================================================================
# CloudPulse Observability – Actuator & Prometheus Metrics
#
# The management server runs on the SAME port as the app (9090) since
# BodhGanga's architecture uses a single port.  The /actuator/prometheus
# endpoint is protected by Spring Security in production (see SecurityConfig).
#
# In the Kubernetes Deployment, a dedicated 'management' Service port
# routes Prometheus scraping to /actuator/prometheus.
# =============================================================================
management.endpoints.web.exposure.include=health,info,prometheus,metrics,loggers
management.endpoints.web.base-path=/actuator
management.endpoint.health.show-details=always
management.endpoint.health.probes.enabled=true
management.endpoint.prometheus.enabled=true
management.health.mail.enabled=false

# Prometheus export settings
management.prometheus.metrics.export.enabled=true
management.prometheus.metrics.export.step=30s

# Tag every metric with application name and environment
management.metrics.tags.application=${spring.application.name}
management.metrics.tags.environment=${ENVIRONMENT:production}

# SLO histogram buckets for p50/p95/p99 latency calculation
management.metrics.distribution.percentiles-histogram.http.server.requests=true
management.metrics.distribution.slo.http.server.requests=50ms,100ms,200ms,500ms,1s,2s

# Logging
logging.level.com.bodhganga=INFO
logging.level.org.springframework.security=WARN
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# YouTube API
youtube.api.key=${YOUTUBE_API_KEY:}
youtube.channel.id=${YOUTUBE_CHANNEL_ID:}

# Google Drive Ingestion Pipeline
google.drive.pipeline.enabled=${GOOGLE_DRIVE_PIPELINE_ENABLED:false}
google.drive.credentials.path=${GOOGLE_DRIVE_CREDENTIALS_PATH:classpath:google-credentials.json}
google.drive.source-folder-id=${GOOGLE_DRIVE_SOURCE_FOLDER_ID:}
google.drive.archive-folder-id=${GOOGLE_DRIVE_ARCHIVE_FOLDER_ID:}

# Gemini AI
gemini.api.key=${GEMINI_API_KEY:}
gemini.model=${GEMINI_MODEL:gemini-2.5-flash}
