package com.bodhganga.bodhganga.controllers;

/**
 * UserController — DEPRECATED stub.
 * All user profile operations are handled by ProfileController (/api/profile).
 * This file is intentionally left empty to avoid route conflicts and security issues.
 * The original implementation exposed unauthenticated GET /profile → List<User> which was a critical vulnerability.
 */
public class UserController {
    // Intentionally empty — see ProfileController for all /api/profile endpoints
}
